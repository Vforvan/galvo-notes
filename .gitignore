# ============================================================
# 本仓库只收录**原创内容** + 一个 BSD 许可的参考实现。
# 体积大或涉第三方版权的资料一律不入库，来源与获取方式见 THIRD-PARTY.md。
# ============================================================

# ------------------------------------------------------------
# 第三方版权文档（约 30 MB，179 MB 若含重复副本）
#   LasIA 标准原文、RAYLASE / SCANLAB / SinoGalvo 手册、TI 器件手册、论文……
#   这些是厂商与标准组织的版权文档，公开再分发有风险，请自行从官方渠道获取。
#   忽略后仍会保留本文件与 THIRD-PARTY.md 的说明，不会静默丢失线索。
# ------------------------------------------------------------
99-附件与下载/pdf/
99-附件与下载/pdf/**

# ------------------------------------------------------------
# 网页抓取缓存与原始提取件（约 183 MB，761 个文件）
#   含大量重复 PDF 副本（例如 e1803_manual 在 4 个目录下各一份）、
#   厂商页面正文抓取、以及第三方仓库的整棵源码树副本。
#   笔记已被完整归档，不再随仓库分发。
# ------------------------------------------------------------
99-附件与下载/_调研原始缓存/

# ------------------------------------------------------------
# 第三方源码：只保留 BSD-3-Clause 的 XY2Galvo-rp2040。
# GPL-2.0 的 OPAL-gcode-to-xy2-100 与 GPL-3.0 的 xy2-100-arduino
# 因 copyleft 与本源代码许可不兼容，改为在笔记中给出仓库链接，见 THIRD-PARTY.md。
# ------------------------------------------------------------
99-附件与下载/代码/OPAL-gcode-to-xy2-100/
99-附件与下载/代码/xy2-100-arduino/
99-附件与下载/代码/sigrok-xy2-100-decoder/

# 去 .git 时留下的原件备份（含 .git，会被 git 当成嵌套仓库 gitlink，
# 别人 clone 只得到一个空目录）。核对无误后可自行删除。
#
# 注意这里**不能**写成 `*-orig/`：带 `.git` 的目录在 git 看来是"仓库"而不是
# "目录"，带斜杠的模式匹配不上（`git check-ignore` 会静默不命中）。
*-orig

# ------------------------------------------------------------
# Python 运行产物
# ------------------------------------------------------------
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/

# ------------------------------------------------------------
# Obsidian：保留插件与外观配置（便于别人打开就是同样的视图），
# 但 workspace.json 记录的是**你本机的窗口布局**，每次开合面板都会变，
# 入库只会产生无意义的 diff。
# ------------------------------------------------------------
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache/

# ------------------------------------------------------------
# 编辑器 / 系统
# ------------------------------------------------------------
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini
*.log

# ------------------------------------------------------------
# 仿真与工具产物（波形、综合输出等，需要时再决定是否入库）
# ------------------------------------------------------------
*.vcd
*.lxt
*.lxt2
*.fst
*.vvp
*.out
a.out
